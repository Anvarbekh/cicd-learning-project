name: Python CI/CD Pipeline

on:
  push:
    branches:
      - 'main'
      - 'my-feature-branch'
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install ruff
      - name: Lint with Ruff
        run: ruff check .

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Run tests with Pytest
        run: PYTHONPATH=. pytest

  build-and-push:
    runs-on: ubuntu-latest
    needs: [lint, test]
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set repo name to lowercase
        id: string
        shell: bash
        run: |
          echo "repo_lc=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push the Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ steps.string.outputs.repo_lc }}:latest
            ghcr.io/${{ steps.string.outputs.repo_lc }}:${{ github.sha }}

  deploy_staging:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main'
    environment:
      name: staging
      url: http://staging.example.com
    steps:
      - name: 'Simulate Deploying to Staging'
        env:
          IMAGE_NAME: ghcr.io/${{ steps.string.outputs.repo_lc }}:${{ github.sha }}
          SERVER_IP: ${{ secrets.STAGING_SERVER_IP }}
        run: |
          echo "🚀 Starting deployment to STAGING environment..."
          echo "🚚 Connecting to server at IP: $SERVER_IP"
          echo "📦 Pulling image: $IMAGE_NAME"
          echo "✅ Deployment to staging complete!"

  deploy_production:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name == 'workflow_dispatch'
    environment:
      name: production
      url: https://production.example.com
    steps:
      - name: 'Simulate Deploying to Production'
        env:
          IMAGE_NAME: ghcr.io/${{ steps.string.outputs.repo_lc }}:latest
          SERVER_IP: ${{ secrets.PROD_SERVER_IP }}
        run: |
          echo "🚀 Starting deployment to PRODUCTION environment..."
          echo "🚚 Connecting to server at IP: $SERVER_IP"
          echo "📦 Pulling image: $IMAGE_NAME"
          echo "✅ Deployment to production complete!"